{
  "name": "notes-management-system",
  "private": true,
  "scripts": {
    "build": "cd client && npm install --include=dev && npm run build",
    "postinstall": "cd client && npm install --include=dev && npm run build && cd ../server && npm install"
  },
  "engines": {
    "node": "24.x"
  }
}
